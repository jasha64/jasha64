#设有编号为1~n的n个人按顺时针站成一个圆圈。
#首先从第1个人开始，按顺时针从1开始报数，报到第m个人，令其出列。
#然后再从出列的下一个人开始，按顺时针从1开始报数，报到第m个人，再令其出列……
#如此下去，直到圆圈不再有人为止。求这n个人出列的顺序。

n, m = 20, 7
x = list(range(n))
y = []

cur = 0
while x:
    cur = (cur + m - 1) % len(x)
    y.append(x.pop(cur))

print(y)
